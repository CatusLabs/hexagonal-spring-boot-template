#!/bin/sh
# Maven Wrapper placeholder
# After cloning, generate the full wrapper with:
#   mvn wrapper:wrapper -Dmaven=3.9.14
#
# Or download it from:
#   https://github.com/apache/maven-wrapper

echo "Maven Wrapper not installed. Run: mvn wrapper:wrapper -Dmaven=3.9.14"
exit 1
