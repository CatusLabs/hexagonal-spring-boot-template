name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: catuslabs/cicd-tools/.github/workflows/build-sb-app.yml@main
    permissions:
      contents: read
